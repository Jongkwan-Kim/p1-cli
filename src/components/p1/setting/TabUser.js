import React from 'react';
import { Grid } from '@material-ui/core';
import { withStyles } from '@material-ui/styles';

const styles = theme => ({

});

const TabUser = ({ classes }) => {
    return (
        <div>
            TabUser
        </div>
    )
};

export default withStyles(styles)(TabUser);