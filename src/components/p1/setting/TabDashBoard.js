import React from 'react';
import { Grid } from '@material-ui/core';
import { withStyles } from '@material-ui/styles';

const styles = theme => ({

});

const TabDashBoard = ({ classes }) => {
    return (
        <div>
            TabDashBoard
        </div>
    )
};

export default withStyles(styles)(TabDashBoard);